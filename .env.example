# ===========================================
# DESA DIGITAL - ENVIRONMENT CONFIGURATION
# ===========================================
# File ini digunakan oleh docker-compose.prod.yml
#
# Cara pakai:
#   1. Salin file ini → .env
#   2. Sesuaikan nilai di bawah
#   3. Deploy via Portainer (lihat README)

# ── Database ──
POSTGRES_USER=postgres
POSTGRES_PASSWORD=GantiDenganPasswordKuat123!
POSTGRES_DB=desa_digital

# ── Security (WAJIB diganti untuk production!) ──
# Generate di terminal: openssl rand -hex 32
# Atau online: https://generate-random.org/api-key-generator
JWT_SECRET=ganti_dengan_random_string_minimal_32_karakter
ENCRYPTION_KEY=ganti_dengan_random_hex_64_karakter

# ── Domain & CORS ──
# Tanpa domain (akses via IP): http://IP_SERVER
# Dengan domain: https://cctv.desaanda.com
# Multi-origin (pisah koma): http://IP_SERVER,https://cctv.desaanda.com
# Izinkan semua (dev only): *
CORS_ORIGIN=http://localhost

# ── Akun Superadmin (dibuat otomatis saat pertama kali) ──
ADMIN_USERNAME=superadmin
ADMIN_PASSWORD=Admin123!
ADMIN_NAME=Super Admin

# ── Data Demo (true = isi contoh desa/RT/kamera, false = kosong) ──
SEED_DEMO_DATA=false

# ── Telegram Notifikasi (opsional, bisa diisi dari menu Pengaturan) ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Cloudflare Tunnel (opsional) ──
CLOUDFLARE_TUNNEL_TOKEN=
